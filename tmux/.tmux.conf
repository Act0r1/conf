# ============================================================================
# GENERAL SETTINGS
# ============================================================================

set -g default-shell /bin/zsh
set-option -g prefix C-a
set -g default-terminal "tmux-256color"
set -sg terminal-overrides ",*:RGB"
set -g mode-style fg=magenta,bg=default


# ============================================================================
# YAZI INTEGRATION (Image Preview Support)
# ============================================================================

set -g allow-passthrough on
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM
set -g visual-activity off


# ============================================================================
# THEME & APPEARANCE
# ============================================================================

# Pane borders
set -g pane-border-lines simple
set -g pane-border-style fg="#3a3f5c"
set -g pane-active-border-style fg="#9ab6ff"

# Status bar
set -g status-style bg=default,fg="#dfe6ff"
set -g status-left ""
set -g status-right "#[fg=#5a607e]#S"
set-option -g status-justify absolute-centre

# Window status
set -g window-status-format "●#W"
set -g window-status-current-format "●"
set -g window-status-current-style "#{?window_zoomed_flag,fg=#ffe08a,fg=#d6aaff},nobold"
set -g window-status-bell-style "fg=#ff8fab,nobold"

# Messages
set -g message-style bg=default,fg="#9eeeff"


# ============================================================================
# BEHAVIOR SETTINGS
# ============================================================================

# Window and pane management
setw -g allow-rename off
setw -g automatic-rename off
set-option -g renumber-windows on
set -g base-index 1
setw -g pane-base-index 1

# Session behavior
set -g detach-on-destroy off

# History and performance
set -g history-limit 500000
set -s escape-time 0
set -sg repeat-time 100
set -s focus-events on

# Input settings
set -g mouse on
setw -g mode-keys vi
setw -g xterm-keys on


# ============================================================================
# KEY BINDINGS
# ============================================================================

# Unbind default keys
unbind C-b
unbind n
unbind p
unbind x

# Prefix and Reload
bind-key C-a send-prefix
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"

# Session Management
bind n switch-client -n
bind p switch-client -p
bind s display-popup -E "tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse | xargs tmux switch-client -t"
bind-key X confirm-before -p 'kill-session #S? (y/n)' kill-session

# Window Management
bind c new-window
bind Tab last-window
bind x kill-pane

# Window Navigation (Alt + Number)
bind-key -n M-1 select-window -t 1
bind-key -n M-2 select-window -t 2
bind-key -n M-3 select-window -t 3
bind-key -n M-4 select-window -t 4
bind-key -n M-5 select-window -t 5
bind-key -n M-6 select-window -t 6
bind-key -n M-7 select-window -t 7
bind-key -n M-8 select-window -t 8
bind-key -n M-9 select-window -t 9

# Pane Management
bind - split-window -v
bind _ split-window -h

# Pane Navigation (vim-style)
bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R

# Pane Swapping
bind > swap-pane -D
bind < swap-pane -U

# Copy Mode (Vi-style)
bind-key -Tcopy-mode-vi MouseDragEnd1Pane send-key -X copy-pipe "xclip -in -r -selection clipboard"
bind-key -Tcopy-mode-vi Y send -X copy-pipe "xclip -in -selection clipboard -r"

# Utility Keybindings
bind-key -n M-l send-keys "clear" C-m
bind-key m send-keys "cmatrix" C-m
bind-key y display-popup -d "#{pane_current_path}" -E "yazi"
bind-key b send-keys "bws" C-m


# ============================================================================
# PLUGINS
# ============================================================================

set -g @t-fzf-prompt '  '
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'joshmedeski/t-smart-tmux-session-manager'
set -g @plugin 'wfxr/tmux-fzf-url'
set -g @plugin 'sainnhe/tmux-fzf'

run '~/.tmux/plugins/tpm/tpm'


# ============================================================================
# COMMENTED OUT / ARCHIVED SETTINGS
# ============================================================================

# Alternative theme configurations
# set -g pane-border-lines simple
# set -g pane-border-style fg=black,bright
# set -g pane-active-border-style fg=magenta
#
# # Theme: status
# set -g status-style bg=default,fg=pink,bright
# set -g status-left ""
# set -g status-right "#[fg=black,bright]#S"
#
# # Theme: status (windows)
# set -g window-status-format "●#W"
# set -g window-status-current-format "●"
# set -g window-status-current-style "#{?window_zoomed_flag,fg=yellow,fg=magenta,nobold}"
# set -g window-status-bell-style "fg=red,nobold"
# set -g message-style               bg=default,fg=magenta

# Alternative window navigation
# bind -r C-h previous-window
# bind -r C-l next-window

# Alternative pane selection
#bind -r h select-pane -L

# Vim navigator custom mappings
# set -g @vim_navigator_mapping_left "C-h"  # use C-h and C-Left
# set -g @vim_navigator_mapping_right "C-l"
# set -g @vim_navigator_mapping_up "C-k"
# set -g @vim_navigator_mapping_down "C-j"
# set -g @vim_navigator_mapping_prev ""
# set -g @vim_navigator_prefix_mapping_clear_screen ""

# Alternative plugins
# set -g @plugin 'aserowy/tmux.nvim'
# set -g @plugin 'catppuccin/tmux'
# set -g @catppuccin_flavour 'mocha'
# set -g @catppuccin_window_default_text "#W"
# set -g @catppuccin_window_current_text "#W"

# Alternative settings
# set -gq allow-passthrough on
# bind c new-window -c "#{pane_current_path}"
# set-option -g status-justify center
